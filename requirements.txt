# === Core Framework ===
Flask==2.3.3                   # The web framework
Flask-SQLAlchemy==3.0.5        # ORM (database models as Python classes)
Flask-Migrate==4.0.5           # Database migrations (ALTER TABLE without SQL)
Flask-Login==0.6.3             # Session management for logged-in users
Werkzeug==2.3.7                # Password hashing + HTTP utilities
psycopg2-binary==2.9.7         # PostgreSQL driver
GeoAlchemy2==0.15.2            # PostGIS integration for spatial queries
python-dotenv==1.0.0           # Load .env file into environment

# === Security ===
Flask-WTF==1.2.1               # CSRF protection on all forms
Flask-Limiter==3.5.1           # Rate limiting (brute-force protection)
limits==3.13.0                 # Storage backend for Flask-Limiter
cryptography==42.0.5           # Fernet encryption for sensitive fields
itsdangerous==2.1.2            # Signed tokens for email verification links
bleach==6.1.0                  # HTML sanitization for user input
pytz==2024.1                   # Timezone conversion and DST handling

# === Email + Scheduling ===
resend==2.23.0                 # Resend.com email API (3,000 free/month)
APScheduler==3.10.4            # Background scheduler for reminder emails

# === Document Verification ===
boto3==1.34.69                 # AWS S3-compatible client (works with Cloudflare R2)
python-magic==0.4.27           # File type detection (prevents fake extensions)

# === Payments ===
stripe==8.4.0                  # Stripe payment processing

# === Redis & Caching ===
redis==5.0.4                   # Redis client (works with Upstash free tier)

# === JWT (Microservices Auth) ===
PyJWT==2.8.0                   # JSON Web Token encoding/decoding
requests==2.31.0               # HTTP client for inter-service calls

# === Scaling & Concurrency ===
gevent==24.2.1                 # Coroutine-based concurrency (greenlets)
gevent-websocket==0.10.1       # WebSocket support for gevent workers
Flask-Session==0.8.0           # Server-side session storage (Redis backend)
celery[redis]==5.4.0           # Distributed task queue for background jobs

# === AI (Chatbot) ===
openai>=1.30.0

# === PDF Export ===
fpdf2>=2.7.0

# === Testing ===
pytest==8.1.1
pytest-cov==5.0.0
